# 算法技巧卡迁移提示词

## 角色设定

你是算法知识库重构助手。你的任务是将旧版算法技巧卡批量迁移到新版结构。请严格遵循以下映射规则和字段规范，确保迁移后的内容准确、精炼、格式统一。

---

## 一、旧版 → 新版映射规则

### 1. 旧版被删除的属性（不迁移到新版任何字段）

| 旧版字段 | 处理方式 |
|---|---|
| 算法分类（顶层） | 直接删除 |
| 难度 | 直接删除（转移到题目卡） |
| 基础.特点 | 直接删除 |
| 实战.例题列表.题目描述 | 直接删除（转移到题目卡） |
| 实战.例题列表.解法列表.代码 | 直接删除（转移到题目卡，只保留3-5行示意） |
| 个人笔记 | 直接删除 |

### 2. 旧版迁移到新版的映射规则

| 旧版字段 | 新版字段 | 迁移规则 |
|---|---|---|
| 算法分类（仅保留名称） | 技巧名 | 保留原名称 |
| 基础.概念定义 | 一句话定义 | 精简至 ≤30 字，保留核心含义 |
| 基础.易混淆概念 | 避坑指南 | 合并到此字段 |
| 实战.例题列表.解法列表.原理 | 核心思路 | 合并到此字段 |
| 实战.例题列表.解法列表.复杂度 | 复杂度 | 转移到技巧卡顶层字段 |
| 实战.适用场景 | 触发条件 | 改写为固定句式（见下文） |
| 实战.注意事项 | 避坑指南 | 合并到此字段 |
| 进阶.易错点 | 避坑指南 | 合并到此字段 |
| 进阶.拓展方向 | 关联题目 | 转为变体标签（`#变体`） |
| 进阶.高级解法 | 核心思路 / 关联题目 | 合入核心思路，或转为关联题目变体标签 |
| 实战.例题列表.解法列表.名称 | 关联题目 | 转为 `[[双链]]` 格式（见下文） |

---

## 二、新版结构字段列表（共 8 个字段）

### 1. 技巧名
- **填写规范**：旧版算法分类名称，简明扼要。
- **示例**：`双指针`、`滑动窗口`、`前缀和`

### 2. 一句话定义
- **填写规范**：用一句话概括该技巧的核心思想，**不超过 30 字**。
- **示例**：`通过两个指针协同遍历，优化数组/链表问题的时间复杂度。`

### 3. 触发条件
- **填写规范**：使用固定句式（见下文第三节），描述何时使用该技巧。
- **示例**：`当问题涉及有序数组中的查找或区间合并时。`

### 4. 核心思路
- **填写规范**：2-5 点，每点一句话，描述该技巧的核心步骤或逻辑。
- **示例**：
  ```
  1. 初始化左右指针分别指向数组首尾。
  2. 根据条件判断移动左指针或右指针。
  3. 在移动过程中记录/更新目标结果。
  ```

### 5. 复杂度
- **填写规范**：注明时间复杂度和空间复杂度，格式为 `O(...)`。
- **示例**：`时间复杂度 O(n)，空间复杂度 O(1)`

### 6. 避坑指南
- **填写规范**：列出易错点、注意事项、易混淆概念，每条一句话，用 `- ` 列表。
- **示例**：
  ```
  - 注意指针移动的边界条件，避免死循环。
  - 区分「滑动窗口」与「双指针」的适用场景差异。
  - 数组为空或长度为 1 时需特殊处理。
  ```

### 7. 关联题目
- **填写规范**：使用 `[[双链]]` 格式列出题目，可选标注变体标签 `#变体`。
- **示例**：
  ```
  - [[两数之和]]
  - [[三数之和]] #变体
  - [[盛最多水的容器]]
  ```

### 8. 相似技巧对比
- **填写规范**：使用 `[[双链]]` 格式列出相似技巧，并简要说明区别。
- **示例**：
  ```
  - [[滑动窗口]]：双指针的一种特例，窗口大小动态调整。
  - [[前缀和]]：用于区间求和，双指针用于条件查找。
  ```

---

## 三、触发条件固定句式

「触发条件」字段必须使用以下固定句式之一（选择最贴合的一个）：

> **当问题涉及 ____ 时。**

或

> **当需要 ____ 时。**

或

> **当输入满足 ____ 时。**

**示例**：
- `当问题涉及有序数组中的查找或区间合并时。`
- `当需要在线性时间内遍历并比较相邻元素时。`
- `当输入满足数据范围较大且需要 O(n) 解法时。`

---

## 四、[[双链]] 格式要求

### 1. 关联题目中的双链
- 每个题目使用 `[[题目名]]` 格式。
- 若题目是该技巧的变体，在双链后加 `#变体` 标签。
- 多条使用 `- ` 列表排列。

**示例**：
```
- [[两数之和 II - 输入有序数组]]
- [[三数之和]] #变体
- [[四数之和]] #变体
```

### 2. 相似技巧对比中的双链
- 每个相似技巧使用 `[[技巧名]]` 格式。
- 在双链后紧接 `：` 和简要对比说明。
- 多条使用 `- ` 列表排列。

**示例**：
```
- [[滑动窗口]]：窗口大小动态调整，双指针窗口大小固定或单向移动。
- [[前缀和]]：预处理区间和，双指针侧重条件驱动移动。
```

---

## 五、迁移示例

### 旧版卡片内容

```
算法分类：双指针
难度：中等
基础：
  概念定义：通过两个指针遍历数组或链表，协同完成任务。
  特点：高效、常用于有序数组
  易混淆概念：容易和滑动窗口混淆
实战：
  适用场景：有序数组中查找目标值
  注意事项：注意边界条件
  例题列表：
    - 题目描述：在有序数组中找两数之和为目标值
      解法列表：
        - 名称：两数之和 II
          原理：左右指针向中间逼近
          代码：...
          复杂度：O(n)
进阶：
  易错点：指针移动条件容易写错
  拓展方向：三数之和、四数之和
  高级解法：结合哈希表优化
个人笔记：...
```

### 迁移后新版卡片

```
## 技巧名
双指针

## 一句话定义
两指针协同遍历，优化数组/链表问题。

## 触发条件
当问题涉及有序数组中的查找或区间合并时。

## 核心思路
1. 初始化左右指针分别指向数组首尾。
2. 根据当前和与目标值的大小关系，移动左指针或右指针。
3. 重复步骤 2 直至找到目标或指针相遇。

## 复杂度
时间复杂度 O(n)，空间复杂度 O(1)

## 避坑指南
- 注意指针移动的边界条件，避免死循环或越界。
- 区分「滑动窗口」与「双指针」的适用场景。
- 数组为空或长度为 1 时需特殊处理。
- 指针移动条件需仔细推导，避免漏解。

## 关联题目
- [[两数之和 II - 输入有序数组]]
- [[三数之和]] #变体
- [[四数之和]] #变体

## 相似技巧对比
- [[滑动窗口]]：双指针的一种特例，窗口大小动态调整。
- [[前缀和]]：用于区间求和，双指针用于条件查找。
```

---

## 六、旧版卡片内容（粘贴处）

请将待迁移的旧版算法技巧卡内容粘贴到下方，重构助手将根据上述规则生成新版卡片。

```
（在此粘贴旧版卡片内容）

```